"use client";
import Link from "next/link"; import { useParams } from "next/navigation"; import { useAccount,useReadContract } from "wagmi"; import {Nav} from "@/components/Nav"; import {Footer} from "@/components/Footer"; import {ConnectWallet} from "@/components/ConnectWallet"; import {BoardingPass} from "@/components/BoardingPass"; import {POAP_ABI} from "@/lib/abi"; import {contractAddress,DEFAULT_CHAIN} from "@/lib/contract";
export default function ProofPage(){const id=BigInt(useParams<{eventId:string}>().eventId);const {address,chainId:connected}=useAccount();const chainId=connected??DEFAULT_CHAIN.id;const {data:held}=useReadContract({address:contractAddress(chainId),abi:POAP_ABI,functionName:"hasClaimed",args:address?[id,address]:undefined,query:{enabled:Boolean(address)}});return <main><Nav/><div className="reference-page-shell reference-page-shell-narrow"><p className="eyebrow text-accent">VERIFIABLE ATTENDANCE</p><h1 className="reference-page-title">Your POAP Proof.</h1><p className="reference-page-lead">A shareable, verifiable attendance card. The event artwork remains exactly the artwork registered onchain.</p>{!address?<div className="card mx-auto mt-10 max-w-xl p-10 text-center"><p className="font-bold">Connect the wallet that holds this POAP.</p><div className="mt-4"><ConnectWallet/></div></div>:held===false?<div className="card mx-auto mt-10 max-w-xl p-10 text-center"><p className="font-bold">This wallet does not hold POAP #{id.toString()}.</p><Link href={`/event/${id}`} className="mt-4 inline-flex text-sm font-bold text-accent">View event →</Link></div>:held===undefined?<div className="card mt-10 p-10 text-center text-ink/50">Verifying ownership directly from Base…</div>:<div className="mt-10"><BoardingPass eventId={id} owner={address} chainId={chainId}/></div>}<div className="mt-10 rounded-2xl border border-accent/20 bg-accent/5 p-5"><p className="font-black">What makes this proof trustworthy?</p><p className="mt-2 text-sm leading-6 text-ink/55">Ownership is checked against the ERC-1155 contract, while the event artwork and metadata come from the onchain token URI. No claim is accepted merely because this page says so.</p></div></div><Footer/></main>}
